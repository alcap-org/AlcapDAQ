## A test config modules file
# comments start with a hash (#)
debug

# The first section must begin with [MODULES] and contain a list of all the modules we want to run with
[ MODULES ]
analyse_pulses = MakeAnalysedPulses 
detector_pulses = MakeDetectorPulses

# each subsequent section lists the options for each module above
[ analyse_pulses ]
analyse_channels=all
default_fast_generator=MaxBin
default_slow_generator=MaxBin
SiR2-S=MaxBin

[two]
# options are formed by key-value pairs, separated by an equals sign.
option3=8

[three]
option=no_options here

# if we provide options for a module not listed above then they are ignored
[four]
option=ignored section

#####################################

