GeDet()
{
  TCanvas *MuSunCanvas = (TCanvas *) gROOT->GetListOfCanvases()->At(0);
  MuSunCanvas->Divide(3,2);
  
  // Pad 1-3 : amplitude of 3 channels
  TH1* hGeAmplitude[3] = {0};
  for(int i=0; i<3; i++) {
    MuSunCanvas->cd(1+i);
    char name[50]; sprintf(name,"hGeAmplitude_%d",i+1);
    oldhist = gROOT->FindObjectAny(name); if(oldhist) delete oldhist;
<<<<<<< GeDet.C
    hGeAmplitude[i] = 
      ((TH2*)getHist("hGeAmplitude"))->ProjectionX(name,1,2);
=======
    TH2* hGeAmplitudeAll = (TH2*)getHist("hGeAmplitude");
    if(!hGeAmplitudeAll) {
      printf("Couldn't find histogram hGeAmplitude\n");
      return;
    }
    hGeAmplitude[i] = hGeAmplitudeAll->ProjectionX(name,i+1,i+1);
>>>>>>> 1.4
    hGeAmplitude[i]->Rebin(20);
    hGeAmplitude[i]->Draw();
  }
/*
  // Pad 4-6 : timing of 3 channels
  TH1* hGeTime[3];
  for(int i=0; i<3; i++) {
    MuSunCanvas->cd(4+i);
    char name[50]; sprintf(name,"hGeTime_%d",i+1);
    oldhist = gROOT->FindObjectAny(name); if(oldhist) delete oldhist;
    hGeTime[i] = ((TH2*)getHist("hGeTime"))->ProjectionX(name,i+1,i+1);
    hGeTime[i]->Draw();
  }
<<<<<<< GeDet.C

  // Pad 7-9 : not used
*/
=======
>>>>>>> 1.4
}
